[package]
name = "wsx-driver"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Pre-recorded macro pathfinding sequences between game hubs (visual check-point based)"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
rand = "0.8"
thiserror = "2"

wsx-input = { path = "../input" }
wsx-vision = { path = "../vision" }
